/**
 * Renders the application's Home page container.
 *
 * @returns A JSX element containing a div with className "flex min-h-screen home-wrapper" and the text "Home".
 */
export default function Page() {
  return <div className="flex min-h-screen home-wrapper">Home</div>;
}
